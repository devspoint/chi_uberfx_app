# chi_uberfx_app
Simple Go App using Chi and Fx

To run the project, use:
```
$go build
$go run main.go
```

We have 2 endpoints:

```
$curl localhost:8080
```

and

```
$curl localhost:8080/weather
```
